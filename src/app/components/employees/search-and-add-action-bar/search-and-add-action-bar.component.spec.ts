import {SearchAndAddActionBarComponent} from './search-and-add-action-bar.component';
import {render} from "@testing-library/angular";

describe('SearchAndAddActionBarComponent', () => {
  it('should render', async () => {
    await render(SearchAndAddActionBarComponent)
  })
});
